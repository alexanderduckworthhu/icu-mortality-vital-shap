# Optional EDA notebook placeholder

Prefer scripts + `src/` for reproducibility. If you add a notebook:

1. Load `data/sample/demo_vitals_long.csv`
2. Plot missingness by hour
3. Compare vital trajectories for `label==0` vs `label==1`
4. Do **not** paste MIMIC rows into a public notebook

When MIMIC access arrives, keep EDA private; publish only aggregate figures.
